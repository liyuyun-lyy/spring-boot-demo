# Server configuration
server.port=8080

# Application configuration
spring.application.name=calculator-service

# MCP Server configuration has been removed due to compatibility issues
spring.ai.mcp.server.name=calculator-mcp-server